type Query {
  assistantMessages(anonId: ID): [AssistantMessage!]!,
  channels(messageLimit: Int): [Channel!]!,
  messages(channelId: ID!, limit: Int): [Message!]!,
  release(id: ID!): Release,
  releases: [Release!]!
}

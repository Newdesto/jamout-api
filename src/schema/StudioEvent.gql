type StudioEvent {
    id: ID!,
    createdAt: String,
    userId: ID!,
    username: String,
    studioId: ID!,
    studio: String,
    type: String!,
    startDate: Int,
    endDate: Int,
    sessionId: ID!,
  }
